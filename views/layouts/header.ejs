<header class="text-gray-400 bg-gray-900 body-font mb-10">
  <div
    class="container mx-auto flex flex-wrap p-5 flex-col md:flex-row items-center"
  >
    <a
      href="/"
      class="flex title-font font-medium items-center text-white mb-4 md:mb-0"
    >
      <% if(user == null){ %>
      <img
        src="./public/assets/img/logo.png"
        class="w-10 h-10 text-white object-contain"
      />
      <span class="ml-3 text-xl">Flash Mate</span>
      <% } else{ %>

      <img
        src="<%= user.avatar.large %>"
        class="w-10 h-10 text-white rounded-full"
      />
      <span class="ml-3 text-xl" id="username"><%=user.username%></span>
      <span class="ml-3 text-base"> Your steam id : <%= steamid %> </span>
      <% } %>
    </a>

    <nav
      class="md:ml-auto flex flex-wrap items-center text-base justify-center"
    ></nav>
    <a
      href="<% if(user == null){%> /authenticate <% }else{ %> /logout <%}%>"
      class="inline-flex items-center bg-gray-800 border-0 py-1 px-3 focus:outline-none hover:bg-gray-700 rounded text-base mt-4 md:mt-0"
    >
      <% if(user == null){%> login with steam <% }else{ %> logout <%}%>
      <svg
        fill="none"
        stroke="currentColor"
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="2"
        class="w-4 h-4 ml-1"
        viewBox="0 0 24 24"
      >
        <path d="M5 12h14M12 5l7 7-7 7"></path>
      </svg>
    </a>
  </div>
</header>
