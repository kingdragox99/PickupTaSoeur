<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PickupTaSoeur</title>
  </head>
  <body>
    <main>
      <div>
        <% if(user == null){ %>
        <h1>not login</h1>
        <a href="/authenticate">Login</a>
        <% } else{ %>
        <h1>Salut <%=user.username%></h1>
        <h1>T'on steam id est <%=user.steamid%></h1>
        <a href="/logout">Logout</a>
        <% } %>
      </div>
    </main>

    <footer></footer>
  </body>
</html>
